# API Configuration
# Set this to your server's IP/hostname
# For local development with Docker:
#   - Android Emulator: use 10.0.2.2
#   - iOS Simulator: use localhost or select.test (if in /etc/hosts)
#   - Physical device: use your computer's local IP (e.g., 192.168.1.100)
EXPO_PUBLIC_API_URL=http://select.test:8001/api/v1

# WebSocket (Laravel Reverb) Configuration
# Should match the same host as API_URL
EXPO_PUBLIC_REVERB_HOST=select.test
EXPO_PUBLIC_REVERB_PORT=8080
EXPO_PUBLIC_REVERB_SCHEME=http
EXPO_PUBLIC_REVERB_APP_KEY=select-key
